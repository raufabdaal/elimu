"use client";

export default function DemoBanner() {
  return (
    <div className="fixed top-0 left-0 right-0 z-[60] bg-amber-500 text-white text-center py-1 text-xs font-medium tracking-wider shadow-sm">
      🚧 DEMO MODE — Sample content only
    </div>
  );
}
